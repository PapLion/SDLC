# Roadmap.sh

## GraphQL

### Introduction

Problem grahql solves, thinking in graphs, on frontend, on backend

### GraphQL queries

Fields, variables, aliases, arguments, directives, fragments

### Mutations

Mltiple felds in mutation, operatin name

### Subscriptions

Event based, live queries, @defer/@stream directives

### Schema

Type System, Fields, scalars (int,float,string,boolean,id), enums, objets, list, unions, inferfaces, arguments

### Validation

### Execution

Root fields, resolvers(synchronous, asynchronous, scalar coercion, list)

### Serving over internet

Grahql over http(graphql over http spec, caching, batching, authorization), graphql over websockets(specification,realtime,athorization), graphql over sse (specification, authorization)

### pagination

### Implementations
Go, js, java, goropheme python

### servers

javascrit(httpp, yoga, apollo server, mercurius)

### clients

javascript(http, relay, apollo client, urql)